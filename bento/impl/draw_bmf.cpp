#include "bento/drawing.hpp"
#include "nds/arm9/sassert.h"

namespace nb
{
  void DrawBMF(const BMF &bmf, const char* text, const Vec2 &position)
  {
    sassert(false, "currently not supported!");
  }

  Rect MeasureBMF(const BMF &bmf, const char* text)
  {
    sassert(false, "currently not supported!");
    return Rect();
  }
}